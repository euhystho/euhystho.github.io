---
import { SITE_AUTHOR } from "src/utils/consts";
import Social from "../common/social.astro";
const currentYear = new Date().getFullYear();
interface Props {
  showSocials?: boolean;
}
const { showSocials = true } = Astro.props;
---

<footer>
  {
    showSocials && (
      <div class="flex justify-center">
        <Social />
      </div>
    )
  }
  <section class="bg-tertiary rounded-tl-full rounded-tr-full">
    <h5 class="text-light rounded-tl-lg rounded-tr-full pt-4 pb-4 text-center">
      Copyright © {currentYear}
      {SITE_AUTHOR}. All Rights Reserved.
    </h5>
  </section>
</footer>
